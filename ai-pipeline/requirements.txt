# AI Pipeline Requirements

# Core
numpy>=1.24.0
opencv-python-headless>=4.8.0
pillow>=10.0.0

# YOLO Detection
ultralytics>=8.0.0

# OCR
kreuzberg>=3.0.0
paddlepaddle>=2.5.0
onnxruntime>=1.16.0

# LLM
google-generativeai>=0.3.0
pydantic>=2.0.0
requests>=2.31.0

# TTS
# gTTS is optional and omitted from the default install because it conflicts
# with newer Click versions pulled in by the OCR toolchain on Python 3.13.
# Vieneu is installed separately in Dockerfile because it needs a custom package index.

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Async
asyncio>=3.4.3
aiohttp>=3.9.0
